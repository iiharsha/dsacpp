#include "../template.h"

class Solution {
public:
  string minWindow(string s, string t) {
    string res = s + t;
    return res;
    ;
  }
};

int main(void) {
  fast;

  Solution sol;

  string s = "ADOBECODEBANC";
  string t = "ABC";
  auto res = sol.minWindow(s, t);

  cout << res << endl;

  return 0;
}
